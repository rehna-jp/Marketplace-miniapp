# RouteMap

**Type**: interface

Route matcher that maps URL patterns to handlers
Pattern format: "{METHOD} {path}" where METHOD is HTTP method and path is the route

## Type Definition

```typescript
import { RouteMap } from '@/neynar-web-sdk/nextjs';

interface RouteMap { ... }
```
